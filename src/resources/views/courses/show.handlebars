<div class="mt-4 row">
    <div class="col-lg-3">
        <h1>{{course.name}}</h1>
        <p>{{course.description}}</p>
        <h5>Level: {{course.level}}</h5>
    </div>
    <div class="col-lg-9">
        <iframe class="ps-5"  width="100%" height="515" src="https://www.youtube.com/embed/{{course.videoId}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    </div>
</div>